<UserControl x:Class="SecureBudgetManager.App.Views.SavingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,20">
        <StackPanel>
            <TextBlock Style="{StaticResource PageKicker}" Text="{Binding Kicker}"/>
            <TextBlock Style="{StaticResource PageTitle}" Text="{Binding Title}"/>
            <TextBlock Style="{StaticResource PageLead}" Text="{Binding Summary}"/>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Sinking fund"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Name"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Fund name"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Purpose"/>
                    <ComboBox Style="{StaticResource FormComboBox}" ItemsSource="{Binding PurposeOptions}" DisplayMemberPath="Name" SelectedValuePath="Value" SelectedValue="{Binding Purpose}" AutomationProperties.Name="Fund purpose"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Current balance"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding CurrentBalance, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Fund balance"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Target amount"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding TargetAmount, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Fund target"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Target date"/>
                    <DatePicker Style="{StaticResource FormDatePicker}" SelectedDate="{Binding TargetDate}" AutomationProperties.Name="Fund target date"/>
                    <Button Style="{StaticResource PrimaryButton}" Margin="0,12,0,0" HorizontalAlignment="Left" Content="Save fund" Command="{Binding AddFundCommand}" AutomationProperties.Name="Save fund"/>
                    <TextBlock Style="{StaticResource HintText}" Foreground="{StaticResource Brush.Danger}" Text="{Binding ErrorMessage}"/>
                    <TextBlock Style="{StaticResource HintText}" Text="{Binding StatusMessage}"/>
                </StackPanel>
            </Border>

            <ItemsControl ItemsSource="{Binding Funds}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListRow}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" Foreground="{StaticResource Brush.Text}" FontWeight="SemiBold"/>
                                    <TextBlock Style="{StaticResource HintText}" Text="{Binding Purpose}"/>
                                    <TextBlock Style="{StaticResource HintText}" Text="{Binding Progress}"/>
                                    <TextBlock Style="{StaticResource HintText}" Text="{Binding WeeklyNeed}"/>
                                </StackPanel>
                                <Button Grid.Column="1" Style="{StaticResource DangerButton}" Padding="10,6" FontSize="12" Content="Remove"
                                        Command="{Binding DataContext.RemoveFundCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="Remove fund"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Financial goal"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Name"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding GoalName, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Goal name"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Target"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding GoalTarget, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Goal target"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Already saved"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding GoalCurrent, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Goal current"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Weekly contribution"/>
                    <TextBox Style="{StaticResource FormTextBox}" Text="{Binding GoalContribution, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Goal contribution"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Deadline"/>
                    <DatePicker Style="{StaticResource FormDatePicker}" SelectedDate="{Binding GoalDate}" AutomationProperties.Name="Goal deadline"/>
                    <Button Style="{StaticResource PrimaryButton}" Margin="0,12,0,0" HorizontalAlignment="Left" Content="Save goal" Command="{Binding AddGoalCommand}" AutomationProperties.Name="Save goal"/>
                </StackPanel>
            </Border>

            <ItemsControl ItemsSource="{Binding Goals}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListRow}">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource Brush.Text}" FontWeight="SemiBold"/>
                                <TextBlock Style="{StaticResource HintText}" Text="{Binding Progress}"/>
                                <TextBlock Style="{StaticResource HintText}" Text="{Binding Deadline}"/>
                                <TextBlock Style="{StaticResource HintText}" Text="{Binding Note}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Style="{StaticResource Card}" Margin="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Trade-offs"/>
                    <TextBlock Style="{StaticResource CardBody}" Text="{Binding ConflictText}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
