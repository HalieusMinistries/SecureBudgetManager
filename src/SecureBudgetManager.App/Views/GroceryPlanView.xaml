<UserControl x:Class="SecureBudgetManager.App.Views.GroceryPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:interaction="clr-namespace:SecureBudgetManager.App.Interaction"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="24,20"
                  interaction:ScrollRestore.VerticalOffset="{Binding ListScrollOffset, Mode=TwoWay}">
        <StackPanel>
            <TextBlock Style="{StaticResource PageKicker}" Text="{Binding Kicker}"/>
            <TextBlock Style="{StaticResource PageTitle}" Text="{Binding Title}"/>
            <TextBlock Style="{StaticResource PageLead}" Text="{Binding Summary}"/>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Which plan"/>
                    <ComboBox Style="{StaticResource FormComboBox}"
                              ItemsSource="{Binding PlanKinds}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Value"
                              SelectedValue="{Binding SelectedKind}"
                              AutomationProperties.Name="Grocery plan to show"/>
                    <TextBlock Style="{StaticResource CardBody}" Margin="0,10,0,0" TextWrapping="Wrap" Text="{Binding PlanSummary}"/>
                    <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding AssistanceSummary}"/>
                    <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding FallbackSummary}"/>
                    <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding EmergencySummary}"/>
                    <Button Style="{StaticResource SecondaryButton}"
                            Margin="0,12,0,0"
                            HorizontalAlignment="Left"
                            Content="Create this plan from local guidance"
                            Command="{Binding CreatePlanCommand}"
                            AutomationProperties.Name="Create this plan from local guidance"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="0"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource CardTitle}" Text="Categories" VerticalAlignment="Center" Margin="0"/>
                        <Button Grid.Column="1"
                                Style="{StaticResource SecondaryButton}"
                                Content="Add category"
                                Command="{Binding AddCategoryCommand}"
                                AutomationProperties.Name="Add the new grocery category"/>
                    </Grid>
                    <TextBlock Style="{StaticResource HintText}"
                               Text="Click a category to select it. Double-click, press Enter, or choose Edit to change its weekly limit. Add a custom name below if you need a new group."/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Name for a new custom category"/>
                    <TextBox Style="{StaticResource FormTextBox}"
                             Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}"
                             AutomationProperties.Name="Name of the new grocery category"/>
                    <ItemsControl ItemsSource="{Binding Categories}" Margin="0,8,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource SelectableListRow}"
                                        Focusable="True"
                                        AutomationProperties.Name="{Binding Name}">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      CommandParameter="{Binding}"/>
                                        <MouseBinding MouseAction="LeftDoubleClick"
                                                      Command="{Binding DataContext.OpenCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      CommandParameter="{Binding}"/>
                                        <KeyBinding Key="Return"
                                                    Command="{Binding DataContext.OpenCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"/>
                                    </Border.InputBindings>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" MinWidth="0"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                            <TextBlock Foreground="{StaticResource Brush.Text}" FontWeight="SemiBold">
                                                <Run Text="{Binding Name, Mode=OneWay}"/>
                                                <Run Text=" · "/><Run Text="{Binding Essential, Mode=OneWay}"/>
                                            </TextBlock>
                                            <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap">
                                                <Run Text="Limit "/><Run Text="{Binding WeeklyLimit, Mode=OneWay}"/>
                                                <Run Text=" · spent "/><Run Text="{Binding Spent, Mode=OneWay}"/>
                                                <Run Text=" · remaining "/><Run Text="{Binding Remaining, Mode=OneWay}"/>
                                            </TextBlock>
                                            <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding Ranges}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource CompactButton}"
                                                    Content="Edit"
                                                    Command="{Binding DataContext.OpenCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    AutomationProperties.Name="Edit grocery category"/>
                                            <Button Style="{StaticResource DangerButton}"
                                                    Padding="10,6"
                                                    FontSize="12"
                                                    Content="Remove"
                                                    Command="{Binding DataContext.RemoveCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    AutomationProperties.Name="Remove this category"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="0"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource CardTitle}" Text="Food assistance" VerticalAlignment="Center" Margin="0"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Style="{StaticResource SecondaryButton}"
                                    Content="Add assistance"
                                    Command="{Binding BeginAddAssistanceCommand}"
                                    Visibility="{Binding CanAddAssistance, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    AutomationProperties.Name="Add assistance"/>
                            <Button Style="{StaticResource SecondaryButton}"
                                    Content="Edit"
                                    Command="{Binding BeginEditAssistanceCommand}"
                                    Visibility="{Binding CanEditAssistance, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    AutomationProperties.Name="Edit assistance"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Style="{StaticResource CardBody}"
                               Text="Assistance reduces the cash needed for the categories it actually supplies. It is never counted as income, and never assumed to be permanent."/>
                    <TextBlock Style="{StaticResource HintText}"
                               Text="Click the assistance summary to select it. Double-click, press Enter, or choose Add assistance or Edit to open it."/>
                    <Border Style="{StaticResource SelectableListRow}"
                            Focusable="True"
                            AutomationProperties.Name="Food assistance summary">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                          Command="{Binding SelectAssistanceCommand}"/>
                            <MouseBinding MouseAction="LeftDoubleClick"
                                          Command="{Binding OpenAssistanceCommand}"/>
                            <KeyBinding Key="Return"
                                        Command="{Binding OpenAssistanceCommand}"/>
                        </Border.InputBindings>
                        <StackPanel>
                            <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding AssistanceSummary}"/>
                            <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding FallbackSummary}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" Margin="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="Warnings"/>
                    <ItemsControl ItemsSource="{Binding Warnings}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Style="{StaticResource HintText}" TextWrapping="Wrap" Text="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Style="{StaticResource HintText}" Text="{Binding StatusMessage}"/>
                    <TextBlock Style="{StaticResource HintText}" Foreground="{StaticResource Brush.Danger}" Text="{Binding ErrorMessage}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
